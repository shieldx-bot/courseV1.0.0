# Ascendly — age public key for SOPS secrets (Phase 8 NV5)
# Recipients of devops/secrets/secrets.<env>.enc.yaml are encrypted to this key.
# The matching AGE-SECRET-KEY-* private key is NEVER committed — it lives in the
# deployer's ~/.config/sops/age/keys.txt and in the GitHub Actions secret
# `AGE_SECRET_KEY` (used by release.yml / deploy.sh).
age1cn2x22y4xn5jflxe6539ujtc52m38tyjyfnv4nygva4wym7975kskt4smr
