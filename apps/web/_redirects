# Cloudflare Pages Redirects for SPA
# Format: source destination status

# API routes - handled by Worker (not Pages)
/api/v1/*  https://api.ascendly.io/api/v1/:splat  301

# SPA fallback - all other routes serve index.html
/*  /index.html  200